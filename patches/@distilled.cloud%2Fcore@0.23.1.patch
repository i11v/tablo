diff --git a/src/client.ts b/src/client.ts
index ce70f463dc77c82f444143af23576f4291bfa507..d5d5e0bc5e08efd87dd643922b4d5709be3596bc 100644
--- a/src/client.ts
+++ b/src/client.ts
@@ -767,6 +767,16 @@ export const makeAPI = <Creds>(config: ClientConfig<Creds>) => {
               () => response.text.pipe(Effect.map((text) => text as unknown)),
             ),
           );
+          // PATCH (alchemy-run/distilled#344): Cloudflare answers some 2xx
+          // DELETEs (e.g. DELETE /workers/domains/{id}) with an empty/`null`
+          // body. Decoding that empty body against the envelope schema fails
+          // and surfaces as `CloudflareHttpError: null`, aborting the whole
+          // deploy/destroy. Treat an empty 2xx body as no-content, mirroring
+          // the `204` branch above. Drop this once #344 ships.
+          if (rawBody === null || rawBody === undefined || rawBody === "") {
+            if (outputSchema.ast._tag === "Unknown") return "";
+            return undefined;
+          }
           let responseBody = config.transformResponse
             ? config.transformResponse(rawBody)
             : rawBody;
